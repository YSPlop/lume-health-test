"use client";

import styles from "../styles/hero.module.css";

const Hero = () => {

    return (
        <div className={styles.backgroundColor}>
            
        </div>
    );

};
export default Hero;